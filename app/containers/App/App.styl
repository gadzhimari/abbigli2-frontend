html, body {
  display: flex;
  flex-direction: column;
}

body, .content-wrapper {
  flex-grow: 1;
}

.disable-scroll {
  overflow: hidden;
}

@media (max-width: 767px) {
   .disable-scroll {
        position:fixed;
   }
}