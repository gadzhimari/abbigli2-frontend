.cards-wrap.my-showcase.legacy {
  .tag-card__author{
    display: none
  }
}

.profile_content__error {
  text-align: center;
}

.main {
  position: relative;
}

.main__overlay {
  position: absolute;
  width: 100%;
  height: 100%;
  left: 0;
  top: 0;
  background-color: rgba(255,255,255,0.5);
  z-index: 100;
}